Following tools should be installed.

sudo apt-get install octave
sudo apt-get install octave-communications

In the terminal, start octave interactive prompt by typing,

octave

Then, in the prompt, give the following lines in order to run the backend

load myfirst.m
myfirst
